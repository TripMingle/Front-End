import MapComponent from './MapComponent';
import TripDate from './TripDate';

const ScheduleInput = () => {
  return (
    <div>
      <MapComponent />
      <TripDate />
    </div>
  );
};

export default ScheduleInput;
